#include <iostream>

using namespace std;

int main (void) 
{
    // a. declaração de variáveis
    int a, b, c;

    // b. 


    // c. atribuir à variável c a soma entre a e b; imprimir em formato hex
    c = a + b;
    cout << "Soma dos valores em formato hexadecimal: " << hex << c << endl;

    // c. atribuir à variável c o produto entre a e b; imprimir em formato octal
    c = a * b;
    cout << "Soma dos valores em formato octal: " << hex << c << endl;


    return 0;
}