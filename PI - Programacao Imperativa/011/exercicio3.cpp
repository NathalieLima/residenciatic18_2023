/*
Faça uma função que receba (por referência) 4 variáveis float e ordena (crescente) os
valores destas variáveis. Depois faça o programa principal para testar a sua função.
*/

#include <iostream>

using namespace std;

float ordenarNumeros(float, float, float, float);

int main (void) 
{
    

    cout << "Antigos valores: ";
    cout << "Valores ordenados: ";


    return 0;
}

float ordenarNumeros(float num1, float num2, float num3, float num4)
{
    float array[4] = {num1, num2, num3, num4};

    for (int i = 0; i < 4; i++)
    {

        int j = 0;

        while (j < 3)
        {
            if (array[i] > 
        }
    }
}